import 'package:flutter/material.dart';

class CRUDCancha extends StatelessWidget {
  const CRUDCancha({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('CRUD Canchas'));
  }
}
